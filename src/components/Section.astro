---
interface Props {
  id: string
	title: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="w-full px-8 py-20 flex flex-col items-center justify-center gap-8 scroll-mt-12">
  <h2 class="px-2 py-2 mb-2 text-4xl font-bold">{title}</h2>
  <slot />
</section>