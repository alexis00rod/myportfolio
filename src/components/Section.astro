---
interface Props {
  id: string
	title: string;
}

const { id, title } = Astro.props;
---

<section id={id} class="w-full px-8 mb-28 flex flex-col items-center justify-center scroll-mt-28">
  <h2 class="mb-8 text-3xl font-medium capitalize text-center">{title}</h2>
  <slot />
</section>